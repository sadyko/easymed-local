// BRANCH_RECORDS_V1 — что именно уезжает соседу.
//
// ПЕРЕЧЕНЬ КОЛОНОК В КОДЕ, как у справочника (catalogue.js) и по той же
// причине: фильтр «уберём лишнее перед отправкой» забывают обновить, когда в
// таблицу добавляют колонку, а перечень — нет, потому что новая колонка просто
// не попадает в выгрузку, пока её сюда не впишут.
//
// Денежные поля visit_services (unit_price, total, invoice_item_id) НЕ
// перечислены намеренно и после Фазы 3: деньги везёт ДОКУМЕНТ —
// invoices/invoice_items, — а не строка визита. invoice_item_id вообще
// локальная ссылка, а unit_price/total на строке визита это КОПИЯ позиции
// счёта, которую держит биллинг; вези мы её отдельно, у соседа появилось бы
// второе, расходящееся мнение о той же сумме.
//
// BRANCH_MONEY_V1 (Фаза 3) — деньги. invoices, invoice_items и payments едут по
// тем же правилам, что и записи (миграция 087), но с двумя отличиями, и за
// каждым стоит настоящая ошибка в отчёте:
//
//   1. НАСТОЯЩЕЕ ВРЕМЯ, а не время приёма. created_at и paid_at перечислены
//      ОБЫЧНЫМИ колонками и уезжают. Записям это было не нужно: визит попадает
//      в списки по visit_date, результат — по entered_at, и то, что created_at
//      у приехавшей строки означает «когда МЫ её приняли», никому не мешало. У
//      денег же created_at счёта и paid_at платежа — единственные даты, по
//      которым считается КАЖДЫЙ денежный отчёт. Не вези мы их, счёт, выписанный
//      в филиале во вторник и доехавший в четверг, лёг бы в выручку ЧЕТВЕРГА, и
//      день в главной клинике не сошёлся бы ни с одним днём в филиале.
//   2. paid_amount НЕ уезжает. Это сумма платежей по счёту, и каждое здание
//      считает её из тех платежей, которые к нему доехали (records.js). Так у
//      каждой базы сходится собственная касса: сумма payments равна
//      invoices.paid_amount в любой момент — равенство, на котором стоят
//      кассовый отчёт и возвраты. Приезжай оно готовым числом, счёт показывал
//      бы «оплачено» рядом с пустой таблицей платежей, пока платёж не доедет.
//      status, наоборот, уезжает: 'debt', 'void' и 'refunded' — не арифметика,
//      а решение человека, вычислить их из сумм нельзя, и посчитанный статус
//      превратил бы отменённый в филиале счёт в долг пациента (разбор — в 087).
//
// НЕ ЕДУТ локальные ссылки: invoices.branch_id, created_by, payer_id,
// admission_id; payments.cashier_id, shift_id. Все они — id ЭТОЙ базы и у
// соседа указывали бы в чужие строки или в пустоту (foreign_keys=ON). Что от
// этого теряет отчёт по чужому зданию, надо знать заранее: на платеже нет имени
// кассира, на счёте нет врача (их справочник едет отдельным каналом и по своим
// ключам), нет разбивки по контрагенту (payer) и привязки к госпитализации, и
// нет внутреннего branch_id — «здание» считается по sync_origin.
//
// lab_results.parameter/ref_low/ref_high (ревью Задачи 4, C3) — имя аналита и
// границы нормы. Без parameter документ-фид (documents.js) группирует
// результаты по visit_service_id+parameter, и панель на 20 показателей
// приехавшая без него схлопывается в одну безымянную строку — не «неполно»,
// а неотличимо от одного анализа.
import { nextStamp, stampAt } from './hlc.js';

export const SHIPPED = {
  patients: [
    'mrn', 'full_name', 'first_name', 'last_name', 'middle_name',
    'date_of_birth', 'gender', 'blood_type', 'phone', 'email', 'national_id',
    'address', 'nationality', 'occupation',
    'emergency_contact_name', 'emergency_contact_phone',
    'allergies', 'chronic_conditions', 'notes', 'active', 'registration_date',
  ],
  // CROSS_BRANCH_CALENDAR_V1 — booked_at, «когда записали». Ставит его тот, кто
  // записал, один раз; получатель его не переписывает. Нужен ровно для одного
  // вопроса, на который обе стороны обязаны ответить ОДИНАКОВО: если в один
  // слот врача записались из двух зданий внутри часа между обменами — кто из
  // них первый. created_at на это не отвечает: у ПРИЕХАВШЕЙ строки он означает
  // «когда МЫ её приняли» (см. шапку файла), то есть в здании A запись из B
  // всегда выглядела бы поздней, а в B — ровно наоборот, и каждое здание
  // считало бы победителем себя.
  //
  // room_id и service_id по-прежнему НЕ ЕЗДЯТ (099 и её тест). Кабинет —
  // принадлежность здания по правилу владельца: «Кабинет 5» соседа в нашей базе
  // означал бы случайную местную комнату или ничего — та же причина, по которой
  // справочник не везёт users.room_id. Длительность приезжает готовым числом в
  // duration_minutes, поэтому и услуга для показа чужой карточки не нужна.
  visits: ['visit_date', 'duration_minutes', 'visit_kind', 'visit_type', 'status', 'notes', 'booked_at'],
  visit_services: ['quantity', 'status'],
  lab_results: [
    'parameter', 'value', 'numeric_value', 'unit', 'reference_range',
    'ref_low', 'ref_high', 'flag', 'notes', 'entered_at', 'verified_at',
  ],
  // BRANCH_MONEY_V1. paid_amount здесь НЕТ — он считается из платежей (см.
  // шапку и records.js recomputePaid). created_at/paid_at есть, и это не
  // недосмотр, а требование отчётов: деньги обязаны попасть в тот день, когда
  // они произошли, а не в день доставки.
  invoices: [
    'invoice_number', 'subtotal', 'discount_amount', 'total_amount',
    'status', 'created_at', 'paid_at',
  ],
  invoice_items: ['description', 'quantity', 'unit_price', 'total', 'created_at'],
  payments: ['amount', 'method', 'notes', 'paid_at', 'created_at'],
};

// Ссылки: колонка → таблица, на которую она смотрит. Уезжает uid родителя, а не
// его локальный id — id у соседа другой (см. миграцию 083).
export const REFS = {
  visits: { patient_id: 'patients' },
  visit_services: { visit_id: 'visits' },
  lab_results: { visit_service_id: 'visit_services' },
  // BRANCH_MONEY_V1. У счёта ДВЕ ссылки на строки, и это первый такой случай:
  // records.js уводит запись в ожидание по ПЕРВОМУ отсутствующему родителю, а
  // дождавшись его, тут же уходит ждать второго. Работает, но лишним кругом —
  // и лишний круг здесь честнее, чем счёт без пациента.
  //
  // visit_id необязателен (счёт за госпитализацию и счёт-депозит визита не
  // имеют), patient_id — NOT NULL: счёт без пациента вставить нельзя, и запись
  // будет ждать столько, сколько нужно.
  invoices: { patient_id: 'patients', visit_id: 'visits' },
  invoice_items: { invoice_id: 'invoices' },
  payments: { invoice_id: 'invoices' },
};

// Ссылки на СПРАВОЧНИК — не по uid, а по коду. services синхронизирует
// catalogue.js (Этап 1), у него своя карта соответствий и никакого uid; id
// одной и той же услуги в двух филиалах разный, поэтому visit_services.service_id
// не может уехать как есть. Код услуги — то единственное, что у обеих сторон
// одинаково (catalogue.js усыновляет местную строку по natural: ['code','name']).
// Без этой ссылки принятая строка лабораторной очереди приезжала бы БЕЗ услуги:
// в очереди появляется работа, о которой неизвестно, что это за анализ.
//
//   колонка → { table, key, ref } — ref это имя поля в refs записи.
//   soft: true — «не нашли ключ, заводим строку без этой ссылки» (см. ниже).
export const CODE_REFS = {
  visit_services: { service_id: { table: 'services', key: 'code', ref: 'service_code' } },
  // CROSS_BRANCH_CALENDAR_V1 — ВРАЧ ЕДЕТ С ЗАПИСЬЮ, ССЫЛКОЙ ПО ЛОГИНУ.
  //
  // До этой строки doctor_id не ехал вовсе, и это было правильно ровно до тех
  // пор, пока календарь показывал только своё здание: локальный id указывал бы
  // у соседа в пустоту. Владелец решил «видеть и записывать в любой филиал», а
  // колонки сетки — это врачи, поэтому запись без врача бесполезна: она падает
  // в дорожку «Не назначено» у всех сразу.
  //
  // ЛОГИН, а не ФИО и не телефон, и выбор тот же самый и по той же причине, по
  // которой его сделал справочник (catalogue.js, natural: ['username']):
  // users.username — единственная колонка таблицы с UNIQUE (001_init, COLLATE
  // NOCASE), то есть единственная, по которой две независимо заведённые
  // установки говорят об ОДНОМ человеке. Однофамильцы есть, телефон меняют, id
  // у каждой установки свой; логин человек вводит сам каждый день, и именно им
  // он входит во втором здании.
  //
  // soft: true — НЕЗНАКОМЫЙ ВРАЧ НЕ ОТМЕНЯЕТ ЗАПИСЬ. Услуга ведёт себя иначе:
  // строка лабораторной очереди без услуги — это работа, о которой неизвестно,
  // что делать, поэтому она ЖДЁТ код в sync_pending. У записи наоборот: время,
  // пациент и статус — это и есть запись, а незнакомый логин значит всего лишь
  // «этого человека в наш справочник ещё не привезли» (сотрудники едут
  // ОТДЕЛЬНЫМ каналом и своим тактом, поэтому обгон нормален). Заставить её
  // ждать значило бы спрятать от регистратуры занятое время — а через 30 дней
  // ожидания выселить запись совсем. Поэтому запись заводится БЕЗ врача, видна
  // в «Не назначено», и следующая же её правка после приезда справочника
  // проставит врача.
  visits: { doctor_id: { table: 'users', key: 'username', ref: 'doctor_login', soft: true } },
  // Позиция счёта — та же ссылка и по той же причине. Название услуги в
  // description у неё уже есть (его пишет биллинг), поэтому даже без
  // разрешённой ссылки счёт читается человеком; ссылка нужна отчёту «выручка по
  // услугам». Если кода в справочнике принимающего нет, позиция ЖДЁТ его в
  // sync_pending — как ждёт строка лабораторной очереди. ШАПКА СЧЁТА при этом
  // приезжает отдельной записью и не ждёт ничего: сумма счёта в отчёте верна
  // даже тогда, когда его разбивка по позициям ещё не полна. Отчётам это надо
  // знать: итог считается по invoices, а не суммированием invoice_items.
  invoice_items: { service_id: { table: 'services', key: 'code', ref: 'service_code' } },
};

// ССЫЛКИ, БЕЗ КОТОРЫХ СТРОКУ ВСЁ РАВНО ЗАВОДЯТ. У записей такого не было:
// visits.patient_id, visit_services.visit_id и lab_results.visit_service_id все
// NOT NULL, поэтому приёмник и уводил запись в ожидание, не найдя родителя.
// invoices.visit_id — первая необязательная ссылка (счёт за госпитализацию и
// счёт-депозит визита не имеют вовсе), и обращаться с ней как с обязательной
// нельзя ДВАЖДЫ:
//
//   • запись без этой ссылки не «кривая» — таких счетов в клинике много, и
//     пропустить их значило бы не показать в главной часть выручки;
//   • ждать ненайденный визит опаснее, чем потерять ссылку. Визиты ездят с
//     Фазы 2, но счёт может ссылаться на визит СТАРШЕ того дня, когда здания
//     связали, — такого визита у соседа нет и не будет. Счёт прождал бы 30
//     дней в sync_pending и был бы выселен: деньги молча исчезли бы из отчёта.
//     Поэтому не найден визит — счёт заводится БЕЗ него: сумма, дата и статус
//     важнее связи с визитом, а связь остаётся пустой и видна как пустая.
export const SOFT_REFS = {
  invoices: ['visit_id'],
};

const TABLES = Object.keys(SHIPPED);

/**
 * КТО ПРАВИЛ КОЛОНКУ ЗДЕСЬ И КОГДА — из sync_authored (Задача 7e).
 *
 * Отдельная таблица, а не журнал, ровно потому, что журнал ЧИСТИТСЯ: подтвердил
 * сосед приём — строка ушла, и наша свежая правка осталась без метки, а
 * следующий блоб соседа со СТАРЫМ значением лёг поверх неё. Здесь же авторство
 * живёт столько же, сколько сама строка.
 *
 * @param {(sql: string) => object} q подготовитель запросов вызывающего (у
 *   сборки порции и у приёма он свой, а таблица одна)
 * @returns {Map<string, string>} колонка → время её последней здешней правки
 */
export function authoredAt(q, tbl, uid) {
  const out = new Map();
  try {
    for (const r of q('SELECT col, at FROM sync_authored WHERE tbl = ? AND uid = ?').all(tbl, uid)) {
      if (r && r.col && r.at) out.set(r.col, r.at);
    }
  } catch { /* таблицы нет (старая база) — авторство просто неизвестно */ }
  return out;
}

// I7 (ревью Задачи 4): без кэша каждая ссылка (REFS + CODE_REFS) и каждый
// снимок строки компилируют СВОЙ SQL заново на КАЖДУЮ запись — на партии
// visit_services в 5000 строк это 15 000 компиляций (замерено ревью), хотя
// различных текстов запроса за всю порцию не больше десятка: они зависят от
// имени таблицы, а таблиц четыре. Кэш живёт один buildBatch — не дольше:
// текст запроса привязан к конкретному db.prepare, а не глобален.
function cachedPrep(db) {
  const cache = new Map();
  return (sql) => {
    let s = cache.get(sql);
    if (!s) { s = db.prepare(sql); cache.set(sql, s); }
    return s;
  };
}

/**
 * Собрать порцию для соседа: по одной записи на изменённую строку.
 *
 * self — буква ЭТОЙ установки (идёт в метку и в origin); peer — буква узла,
 * КОМУ собираем (по нему читается sent_seq). Два имени — две вещи.
 *
 * ТРЕТЬЕ имя у записи — `home`: где строку ЗАВЕЛИ. Оно совпадает с self только
 * у наших собственных строк; чужие мы пересылаем как чужие (см. rowHome ниже и
 * комментарий у самого поля).
 *
 * СРЕЗ НАКОПИТЕЛЬНЫЙ (Задача 7b). Хвост читается от sent_seq — горизонта
 * ПОДТВЕРЖДЁННОГО, а не выложенного, — поэтому запись, о получении которой
 * сосед ещё не отчитался, попадает и в следующий блоб, и в тот, что за ним.
 * Так закрывается пропущенная выгрузка: блоб на релее один и замещается
 * следующим, но его СОДЕРЖИМОЕ повторяется, пока не придёт квитанция.
 *
 * ПРЕДЕЛ У ЭТОГО ЕСТЬ, и он тот же, что у засева, — страница. Если
 * неподтверждённого накопилось больше `limit` строк (сосед молчит вторые
 * сутки, а клиника работает), уезжают САМЫЕ СТАРЫЕ limit строк, остальные —
 * следующими выгрузками. Не теряется ничего: пока квитанции нет, sent_seq
 * стоит, и хвост никуда не девается.
 *
 * Пока сосед ХОЛОДНЫЙ (строки в sync_peers ещё нет) или ЗАСЕИВАЕТСЯ (строка
 * есть, но seed_floor не NULL — ревью Задачи 4, C1), heads читаются
 * ПОСТРАНИЧНО из самих таблиц (+ надгробий) через seedPage, а не из журнала —
 * иначе строки, существовавшие ДО миграции 084, никогда бы не уехали. Иначе —
 * обычный хвост журнала.
 *
 * Каждая запись несёт `data` (ВСЯ строка — соседу, у которого её нет, нужны
 * все колонки) и `changed` — список колонок, которые мы действительно правили
 * с прошлой отдачи. Приёмник применяет и защищает по `changed`, а `data`
 * использует как значения; без этого разделения отправитель объявлял бы себя
 * автором всей строки и стирал соседу его собственные свежие правки (шапка 084).
 *
 * @returns {{records: Array, upto: number, clock: object, seed: object|null}}
 *   upto — при тёплом хвосте: seq, до которого собрано; при засеве — тот же
 *   пол журнала, что и seed.floor (см. markSent).
 *   seed — null у тёплого соседа; иначе {floor, done, tbl, at, id} — состояние
 *   курсора страницы, которое markSent ОБЯЗАН передать дальше без изменений.
 */
export function buildBatch(db, { self, peer, limit = 5000, clock: clockFn = Date.now } = {}) {
  const from = db.prepare('SELECT * FROM sync_peers WHERE node = ?').get(peer);
  // ХОЛОДНЫЙ — это «строки нет» ИЛИ «строка есть, но мы ему ни разу не
  // выкладывались» (last_ok IS NULL). Второе стало возможным с ревью M2:
  // строку заводит и ПРИЁМ, чтобы запомнить квитанцию соседу, который
  // выложился первым. Считай мы такого соседа тёплым — наши существующие
  // строки не уехали бы к нему никогда: журнал их не помнит, а засев не
  // случился бы.
  const seeding = from == null || from.last_ok == null || from.seed_floor != null;

  let heads;
  let seed = null;
  let upto;

  if (seeding) {
    // ПОЛ ЗАМОРАЖИВАЕТСЯ ДО чтения таблиц (Minor 9, ревью Задачи 4): читать
    // его ПОСЛЕ значило бы, что правка, случившаяся между двумя чтениями,
    // получает seq НЕ БОЛЬШЕ пола — а раз sent_seq в конце засева станет этим
    // же полом, такая правка навсегда осталась бы НИЖЕ него и не уехала бы
    // никогда. Читая пол первым, любая правка «в процессе засева» получает
    // seq строго ВЫШЕ него и гарантированно приезжает первой же тёплой
    // порцией после засева (тест «правка не теряется под полом»).
    const floor = (from == null || from.seed_floor == null)
      ? db.prepare('SELECT COALESCE(MAX(seq), 0) AS m FROM sync_journal').get().m
      : from.seed_floor;
    const cursor = (from == null || from.seed_floor == null)
      ? { tbl: null, at: '', id: 0 }
      : { tbl: from.seed_tbl, at: from.seed_at || '', id: from.seed_id || 0 };

    // НАБОР СТРОК ЗАМОРОЖЕН ВМЕСТЕ С ПОЛОМ. Иначе «страница 1», собранная
    // второй раз (сосед не подтвердил первую), — уже другой набор: клиника за
    // это время завела новых пациентов. На этом ломаются оба способа обойтись
    // с повтором — и отсев по номеру, и повторное применение (см. шапку
    // sync_peers в 084). Заведённое позже уедет журналом: его seq выше пола.
    //
    // ФОРМАТ — КАК У created_at, СЕКУНДНЫЙ. Сравнение здесь строковое, а
    // toISOString() даёт миллисекунды: '…:22Z' <= '…:22.563Z' ЛОЖНО ('Z' в
    // таблице ASCII больше точки), и все строки той же секунды выпали бы из
    // засева. Поймано тестами: у соседа не оказывалось вообще ничего.
    const started = (from && from.seed_floor != null && from.seed_started)
      || new Date(clockFn()).toISOString().replace(/\.\d+Z$/, 'Z');

    const page = seedPage(db, limit, cursor, started);
    heads = page.heads;
    // НОМЕР СТРАНИЦЫ (Задача 7b): курсор в базе — ПОДТВЕРЖДЁННОЕ место, и
    // страница собирается от него КАЖДЫЙ раз, пока сосед не отчитается. Значит
    // выкладывается всегда следующая за подтверждённой, а её конец уедет в
    // seed_next_* и переедет в курсор только по квитанции с этим номером.
    // Повторный сбор даёт ТЕ ЖЕ строки: пол заморожен, порядок детерминирован.
    seed = {
      floor, started, done: page.done,
      tbl: page.cursor.tbl, at: page.cursor.at, id: page.cursor.id,
      page: (from && from.seed_floor != null ? from.seed_page : 0) + 1,
    };
    upto = floor;
  } else {
    // Хвост журнала. Последняя запись про каждую строку: у пациента,
    // правленного сто раз, есть одно текущее состояние, а не сто. Голая
    // колонка `at` при GROUP BY + MAX(seq) — из строки с MAX(seq): SQLite
    // это гарантирует (bare-column rule), и именно это нам нужно.
    //
    // А вот КОЛОНКИ — наоборот, со ВСЕХ записей группы, а не с последней:
    // строка, у которой правили сперва телефон, потом адрес, уезжает одной
    // записью, и обе колонки в ней авторские. Взять cols только с MAX(seq)
    // значило бы отдать телефон под чужим авторством — ровно тот дефект, от
    // которого весь этот механизм (см. шапку 084). Собираются они отдельным
    // запросом на строку (COLS_AT_SQL ниже) — вместе с НОМЕРОМ, на котором
    // каждая колонка правилась в последний раз.
    heads = db.prepare(`
      SELECT tbl, uid, MAX(seq) AS seq, at
        FROM sync_journal
       WHERE seq > ?
       GROUP BY tbl, uid
       ORDER BY seq
       LIMIT ?
    `).all(from.sent_seq, limit);
    // ОТ ПОДТВЕРЖДЁННОГО, А НЕ ОТ ВЫЛОЖЕННОГО (Задача 7b): sent_seq двигает
    // только квитанция соседа, поэтому здесь и берётся накопительный срез.
    // ORDER BY seq + LIMIT — это самые СТАРЫЕ группы, то есть срез накрывает
    // журнал сплошь до `upto`, без дыр посередине: всё, что ниже, либо в этом
    // срезе, либо перекрыто более поздней записью о той же строке.
    upto = from.sent_seq;
  }

  // ЧАСЫ ХРАНЯТСЯ, а не заводятся заново на каждую порцию: часы, переведённые
  // назад между двумя синхронизациями, дали бы метку НИЖЕ уже отправленной —
  // приёмник её пропустит, а sent_seq уже ушёл вперёд: правка исчезает молча.
  // Читаем один раз на порцию, не на запись.
  let clock = readClock(db);
  const records = [];
  const q = cachedPrep(db);

  for (const h of heads) {
    if (!seeding) upto = Math.max(upto, h.seq);
    if (!TABLES.includes(h.tbl)) continue;

    const row = q(`SELECT * FROM ${h.tbl} WHERE uid = ?`).get(h.uid);
    // Метка — от ВРЕМЕНИ ПРАВКИ (journal.at / created_at), не от времени
    // отправки: иначе правка в 10:00, отправленная в 10:15, побеждала бы
    // настоящую правку соседа в 10:05. `at` — секундной точности; внутри
    // секунды порядок отдачи.
    //
    // I4 (ревью Задачи 4): испорченная/нечитаемая дата не должна чеканить
    // метку «1970» — такая метка проигрывала бы вообще любой настоящей и
    // молча portила бы соседу порядок слияния. Падаем обратно на часы
    // вызова — как для новой правки без собственной сохранённой метки.
    const atMs = h.at ? Date.parse(h.at) : NaN;
    // ЧАСЫ ПО-ПРЕЖНЕМУ ДВИГАЮТСЯ — но метку авторства больше не выдают
    // (Задача 7d). Их дело осталось прежним: не дать порядку приёма
    // развалиться и запомнить, что в сети видели время новее нашего. А вот
    // ответ на вопрос «чья правка колонки новее» они давали неверный: пол
    // часов поднимается до Date.now() на каждом приёме, поэтому метка,
    // отчеканенная выгрузкой, — это время ОТПРАВКИ, и побеждал не тот, кто
    // правил позже, а тот, кто позже вышел на связь.
    clock = nextStamp(clock, self, Number.isFinite(atMs) ? () => atMs : clockFn);
    // Время правки строки: у тёплого хвоста — из журнала, у засева — из самой
    // строки (updated_at, где он есть; иначе created_at), и НИКОГДА не
    // «сейчас». Иначе поздний засев затирал бы у соседа правки, которые он
    // принял от третьего филиала, — тем и опасен снимок под авторством '*'.
    const rowAtMs = rowEditedAt(row, atMs, clockFn);
    const rowStamp = mintAt(rowAtMs, self, clock);

    if (!row) {
      // Надгробие несёт время УДАЛЕНИЯ (журнал/sync_tombstones.at): правило
      // «удаление проигрывает более поздней правке» обязано считаться на той
      // же оси, что и сами правки.
      records.push({ tbl: h.tbl, uid: h.uid, op: 'del', stamp: rowStamp, origin: self });
      continue;
    }

    const data = {};
    for (const col of SHIPPED[h.tbl]) if (row[col] !== undefined) data[col] = row[col];

    const refs = {};
    for (const [col, parent] of Object.entries(REFS[h.tbl] || {})) {
      const pid = row[col];
      if (pid == null) continue;
      const p = q(`SELECT uid FROM ${parent} WHERE id = ?`).get(pid);
      if (p) refs[col] = p.uid;
    }
    for (const [col, spec] of Object.entries(CODE_REFS[h.tbl] || {})) {
      const pid = row[col];
      if (pid == null) continue;
      const p = q(`SELECT ${spec.key} AS k FROM ${spec.table} WHERE id = ?`).get(pid);
      // Услуга без кода не едет вовсе: приёмник опознаёт её ТОЛЬКО по коду, а
      // подобрать «похожую по названию» значило бы привязать чужую работу.
      if (p && p.k) refs[spec.ref] = p.k;
    }

    // ГДЕ ИМЕННО мы правили каждую колонку — номер журнала её последней
    // правки. Нужен ПРИЁМНИКУ (Задача 7b), и вот зачем. Срез теперь
    // накопительный: пока квитанции нет, он повторяет и то, что сосед давно
    // применил. Вместе со старой записью повторяется и её авторство — а у
    // ВСТАВКИ авторство это '*', вся строка. Сосед, успевший с тех пор
    // поправить у себя адрес, получал бы наш снимок (в котором адреса ещё
    // нет) под свежей меткой и терял свою правку — воспроизведено на двух
    // базах. Имея номер, он отбрасывает ровно те притязания, которые уже
    // учёл (recv_upto), и оставляет только новые.
    const changedAt = seeding ? null : {};
    // МЕТКА НА КАЖДУЮ АВТОРСКУЮ КОЛОНКУ (Задача 7d) — от времени, когда её
    // правили здесь. Приёмник по ней и решает поколоночный спор: сравниваются
    // ОДНИ И ТЕ ЖЕ две строки на обеих сторонах, поэтому победитель у них
    // получается один, и слияние сходится.
    const stamps = {};
    if (seeding) {
      // ЗАСЕВ ОТДАЁТ ЧУЖОЕ АВТОРСТВО КАК ЧУЖОЕ (Задача 7d). Журнала по этой
      // строке у нас нет — есть время самой строки, и им подписываются наши
      // собственные колонки. Но часть колонок мы не правили, а ПРИНЯЛИ от
      // третьего филиала, и их настоящее время лежит в sync_seen: это метка,
      // под которой мы их приняли.
      //
      // Без этого поздний засев был разрушителен, и это уже ловилось: B правит
      // адрес и засевает им C, а следом C засевает A, который про правку B ещё
      // не знает, — и адрес у C откатывается на старый. Подписав чужую колонку
      // её собственной меткой, мы отдаём соседу ровно то, что знаем: «вот это
      // значение, и оно от такого-то времени», а он сам решает, новее оно его
      // или нет.
      const seen = new Map(
        q('SELECT col, stamp FROM sync_seen WHERE tbl = ? AND uid = ?').all(h.tbl, h.uid)
          .map((r) => [r.col, r.stamp])
      );
      // Своё авторство — из sync_authored (Задача 7e): настоящее время правки
      // ИМЕННО этой колонки. Раньше здесь стоял rowStamp от updated_at, то есть
      // «последнее касание строки», и правка заметки старила адрес.
      const mine = authoredAt(q, h.tbl, h.uid);
      // ПОЛ ДЛЯ КОЛОНКИ, ПРО КОТОРУЮ МЫ НЕ ЗНАЕМ НИЧЕГО. У строки, заведённой
      // ЗДЕСЬ, это её created_at — честное «раньше не было». У строки
      // ПРИЕХАВШЕЙ created_at означает совсем другое: когда МЫ её приняли, то
      // есть заведомо позже настоящей правки, которая её и привезла. Поставив
      // его, узел выдавал бы чужому значению метку новее всего на свете и
      // замораживал колонку по всей сети — в прогоне это видно как «адрес
      // больше никогда не меняется» (16 расхождений из 40).
      //
      // Поэтому у приехавшей строки пол — ЭПОХА, доказуемо низкая метка, а не
      // самая ранняя из известных нам (Задача 7f, F-1). Разница видна
      // ровно тогда, когда известных не осталось: sync_seen выселяется по
      // горизонту в 90 дней, и после выселения «самая ранняя известная» — это
      // пусто, то есть пол снова падал на rowStamp, то есть на НАШЕ время
      // приёма. Тогда повторный засев отдавал каждую колонку чужой строки
      // меткой «сегодня», и настоящее, более новое значение соседа проигрывало
      // ей навсегда — тот же дефект, что I-1, только через чистку.
      //
      // Достать горизонт можно и внутри ОДНОГО холодного засева клиники с
      // историей: поздние страницы несут свежие метки, они двигают горизонт, и
      // ранние строки 2020 года выселяются из sync_seen до того, как их
      // страницу переспросят.
      //
      // Про колонку чужой строки, которой нет в sync_seen, мы не знаем НИЧЕГО —
      // и говорим об этом эпохой: сосед без своих данных значение возьмёт,
      // сосед с любым своим оставит своё. Низкая метка безопасна, высокая —
      // ложь. У строки, заведённой ЗДЕСЬ, пол остаётся её created_at: «раньше
      // её не было» — правда, которую мы знаем точно.
      const floorStamp = row.sync_origin == null ? rowStamp : mintAt(0, self, clock);
      const mark = (col) => {
        const ours = mine.has(col) ? mintAt(Date.parse(mine.get(col)), self, clock) : null;
        const theirs = seen.get(col) || null;
        // Позже правили — того и метка.
        let best = floorStamp;
        if (ours && ours > best) best = ours;
        if (theirs && theirs > best) best = theirs;
        stamps[col] = best;
      };
      for (const col of SHIPPED[h.tbl]) mark(col);
      for (const col of Object.keys(REFS[h.tbl] || {})) mark(col);
      for (const spec of Object.values(CODE_REFS[h.tbl] || {})) mark(spec.ref);
      // '*' — про строку целиком (её заведение), и метка у неё своя, строчная.
      stamps['*'] = rowStamp;
    } else {
      const mine = authoredAt(q, h.tbl, h.uid);
      for (const c of q(COLS_AT_SQL).all(h.tbl, h.uid, from.sent_seq)) {
        const colMs = c.at ? Date.parse(c.at) : NaN;
        const colStamp = mintAt(Number.isFinite(colMs) ? colMs : rowAtMs, self, clock);
        for (const part of String(c.cols || '*').split(',')) {
          const col = part.trim();
          if (!col) continue;
          if (!(col in changedAt) || changedAt[col] < c.seq) changedAt[col] = c.seq;
          // Время берём из АВТОРСТВА, а журнальное — запасное: у обеих
          // сторон спор решает одна и та же таблица, и расходиться им нельзя.
          const authored = mine.has(col) ? mintAt(Date.parse(mine.get(col)), self, clock) : null;
          const best = authored && authored > colStamp ? authored : colStamp;
          const prev = stamps[col];
          if (!prev || prev < best) stamps[col] = best;
        }
      }
    }

    records.push({
      tbl: h.tbl, uid: h.uid, op: 'put', stamp: rowStamp, data, refs, origin: self,
      // ГДЕ ЭТУ РАБОТУ СДЕЛАЛИ — отдельно от того, кто её сейчас везёт (ревью
      // Фазы 3, NEW-1).
      //
      // origin — это МЫ: «порцию собрал такой-то», и приёмник сверяет его с
      // тем, чей блоб он забрал (applyBatch отвергает запись, подписанную не
      // тем соседом). Менять его нельзя. Но пересылаем мы и ЧУЖИЕ строки:
      // холодный засев отдаёт таблицы целиком, включая всё, что мы сами когда-то
      // приняли от третьего здания. Такая строка уезжала подписанной только
      // нами — и у соседа садилась как «филиал <мы>»: подпись на карточке,
      // «Здание» в отчёте и буква перечеканенного номера называли дом
      // неправильно, а один и тот же счёт носил в сети столько номеров, сколько
      // было маршрутов доставки. Замерено ревью на трёх зданиях.
      //
      // Отсюда home: НАША СОБСТВЕННАЯ строка — это self, чужая — её sync_origin,
      // то есть тот дом, который мы сами когда-то записали. Поле новое, и сосед
      // старой сборки его просто не увидит; сосед, который его не шлёт, для нас
      // остаётся ровно таким, каким был (records.js, remintLetter).
      home: rowHome(row, self),
      // Метка на колонку. Приёмник сборки до 7d этого поля не знает и читает
      // record.stamp для всех колонок разом — ровно как читал раньше.
      stamps,
      // КАКИЕ колонки мы действительно правили. data — по-прежнему ВСЯ строка:
      // у соседа этой строки может не быть вовсе, и заводить её из трёх
      // изменённых полей нечем (NOT NULL без умолчания). Разница в том, что
      // теперь приёмник знает, где в этом снимке наше авторство, а где просто
      // наша копия его же значений.
      //
      // ПРИБЛИЖЕНИЕ, которое надо назвать вслух: метка на запись одна — время
      // ПОСЛЕДНЕЙ правки строки. Телефон, исправленный в 10:00, и адрес,
      // исправленный в 10:05, уедут оба под меткой 10:05. Точнее было бы
      // хранить метку на колонку (журнал это позволяет), но тогда одна строка
      // — это N меток на проводе и N сравнений на приёме; сегодня цена
      // неточности ограничена: обе колонки правил ОДИН узел, и спорит она
      // только с правкой соседа, попавшей в те же пять минут.
      changed: seeding ? ['*'] : unionCols(changedAt),
      // Приёмник СТАРОЙ сборки этого поля не знает и просто читает changed —
      // ровно как читал до Задачи 7b. Поэтому changed остаётся списком, а не
      // становится картой: обновляются узлы не одновременно.
      ...(seeding ? {} : { changed_at: changedAt }),
    });
  }
  // Сколько строк реально уехало этой страницей засева. Ноль — особый случай,
  // и без него засев встаёт намертво: пустой срез в блоб не кладётся вовсе
  // (publishJournal), сосед его не видит и подтвердить не может, а курсор без
  // подтверждения не двигается. Узел, впервые выложившийся ПУСТЫМ (клиника,
  // где ещё никого не завели), остался бы «в засеве» навсегда, и первый же
  // заведённый пациент не уехал бы никуда: заморозка набора его из засева
  // исключает, а журнал у засеваемого соседа не читается. Ловится сквозным
  // тестом сразу: «срез для C обязан уехать».
  if (seed) seed.rows = records.length;
  return { records, upto, clock, seed };
}

// Форма буквы узла — та же, что NODE_RE в hlc.js и SELF_RE в records.js. Своя
// копия по той же причине, что и там: проверка нужна ДО того, как значение
// попадёт на провод, а не внутри чеканки метки.
const HOME_RE = /^[A-Z]{1,8}$/;

/**
 * ГДЕ СДЕЛАНА РАБОТА (поле `home` записи). Своя строка (sync_origin пуст) —
 * это мы; чужая — тот дом, который мы записали, когда её приняли.
 *
 * Испорченное значение колонки НЕ выдаётся за наше: пустое поле приёмник
 * понимает как «дом не назван» и честно падает на прежнее поведение, а
 * подставленный self объявил бы чужую работу нашей во всей сети.
 */
function rowHome(row, self) {
  const from = row && row.sync_origin;
  if (from == null || from === '') return self;
  const up = String(from).toUpperCase();
  return HOME_RE.test(up) ? up : null;
}

/**
 * Время, которым подписывается СТРОКА. Тёплый хвост знает его из журнала;
 * засев — из самой строки: updated_at там, где он есть (patients, visits), и
 * created_at там, где его нет (visit_services, lab_results). «Сейчас» не
 * подставляется никогда: именно оно и делало поздний засев разрушительным.
 */
function rowEditedAt(row, atMs, clockFn) {
  // ПОЛ — created_at, а НЕ updated_at (Задача 7e, I-1). updated_at один на всю
  // строку: подставив его как время правки КОЛОНКИ, мы объявляли бы, что
  // правка заметки обновила и возраст адреса, — и сосед держал бы устаревший
  // адрес вечно (воспроизведено ревью). Настоящее поколоночное время лежит в
  // sync_authored; сюда оно приходит отдельно, а здесь — только пол для тех
  // колонок, которых здесь не правили ни разу.
  const born = row ? Date.parse(row.created_at || '') : NaN;
  if (Number.isFinite(born) && (!Number.isFinite(atMs) || born > atMs)) return born;
  if (Number.isFinite(atMs)) return atMs;
  // Ни журнал, ни строка не дали читаемой даты — берём часы вызова, как и
  // раньше в этом случае: метка «1970» проиграла бы вообще всему и молча.
  return Math.floor(clockFn());
}

/**
 * Метка от времени правки. Падает обратно на метку часов, если время
 * нечитаемо: пустая строка на проводе хуже приблизительной.
 */
function mintAt(ms, self, clock) {
  try {
    return stampAt(ms, self);
  } catch {
    return clock.stamp;
  }
}

// Колонки строки, номер и ВРЕМЯ их последней правки — по НАБОРАМ, как их записал
// триггер. GROUP BY cols, а не строка за строкой: у пациента, правленного
// тысячу раз, различных наборов два-три, и группировка превращает тысячу
// строк в три ещё в базе.
const COLS_AT_SQL = `
  SELECT cols, MAX(seq) AS seq, MAX(at) AS at FROM sync_journal
   WHERE tbl = ? AND uid = ? AND seq > ?
   GROUP BY cols
`;

/**
 * Ключи карты «колонка → номер» в список для `changed`. '*' поглощает всё:
 * строка уезжает целиком (вставка, засев, появление uid). Пусто — тоже '*':
 * «неизвестно, что менялось», и осторожность здесь дороже точности.
 */
function unionCols(changedAt) {
  const cols = Object.keys(changedAt || {});
  if (!cols.length || cols.includes('*')) return ['*'];
  return cols;
}

// Ранг таблицы — тай-брейк порядка засева, СТРОГО порядок зависимостей REFS:
// patients < visits < visit_services < lab_results. created_at сам ставит
// родителя раньше ребёнка почти всегда (ребёнок создаётся позже), ранг нужен
// только чтобы разрешить редкую ничью created_at (секундная точность) в ТУ ЖЕ
// сторону, а не в произвольную.
//
// BRANCH_MONEY_V1 — деньги встают ПОСЛЕ записей и в своём порядке зависимостей:
// счёт смотрит на пациента и визит, позиция и платёж — на счёт.
const TABLE_RANK = {
  patients: 0, visits: 1, visit_services: 2, lab_results: 3,
  invoices: 4, invoice_items: 5, payments: 6,
};

// Курсор засева одалживает поле seed_tbl как имя фазы, пока идут надгробия —
// ни одна настоящая таблица так не называется, столкновения быть не может.
const TOMB_PHASE = 'sync_tombstones';

// ЗАСЕВ ВЕЗЁТ ВСЮ ИСТОРИЮ, ВКЛЮЧАЯ ДОАПГРЕЙДНУЮ, — и это здесь верно, менять
// нечего. Но у этого есть следствие, о котором надо знать, читая запрос:
// номера, выданные ДО того, как здание получило свою букву (088 — счёту, 080 —
// карте), у двух установок СОВПАДАЮТ, а invoices.invoice_number и patients.mrn
// объявлены UNIQUE. Разбирается это на ПРИЁМЕ — records.js, UNIQUE_TEXT_KEYS:
// занятый номер получает букву здания-автора. Отправитель об этом не знает и
// знать не должен: он отдаёт ровно то, что у него напечатано.
const SEED_PRESENCE_SQL = `
  SELECT tbl, uid, at, id FROM (
    SELECT 'patients' AS tbl, 0 AS rank, uid, created_at AS at, id FROM patients WHERE uid IS NOT NULL
    UNION ALL
    SELECT 'visits' AS tbl, 1 AS rank, uid, created_at AS at, id FROM visits WHERE uid IS NOT NULL
    UNION ALL
    SELECT 'visit_services' AS tbl, 2 AS rank, uid, created_at AS at, id FROM visit_services WHERE uid IS NOT NULL
    UNION ALL
    SELECT 'lab_results' AS tbl, 3 AS rank, uid, created_at AS at, id FROM lab_results WHERE uid IS NOT NULL
    UNION ALL
    SELECT 'invoices' AS tbl, 4 AS rank, uid, created_at AS at, id FROM invoices WHERE uid IS NOT NULL
    UNION ALL
    SELECT 'invoice_items' AS tbl, 5 AS rank, uid, created_at AS at, id FROM invoice_items WHERE uid IS NOT NULL
    UNION ALL
    SELECT 'payments' AS tbl, 6 AS rank, uid, created_at AS at, id FROM payments WHERE uid IS NOT NULL
  )
  WHERE at <= @started
    AND (at > @at OR (at = @at AND (rank > @rank OR (rank = @rank AND id > @id))))
  ORDER BY at, rank, id
  LIMIT @limit
`;

/**
 * Одна страница холодного засева. Форма голов — ТА ЖЕ, что у хвоста журнала
 * ({tbl, uid, seq: 0, at}): buildBatch дальше не различает засев и журнал —
 * put/del решается одним и тем же способом («есть ли сейчас такая строка»).
 *
 * ДВЕ ФАЗЫ СТРОГО ПОСЛЕДОВАТЕЛЬНО: сперва присутствующие строки (по
 * created_at — реальному времени правки), ПОТОМ все надгробия. Не наоборот
 * (ревью Задачи 4, N2 — измерено на живом воспроизведении): buildBatch
 * чеканит метку каждой головы часами HLC, а у HLC пол монотонный и НИКОГДА не
 * опускается. Надгробие несёт время УДАЛЕНИЯ — почти всегда «только что», то
 * есть позже любого created_at из присутствий. Пусти его ПЕРВЫМ — и пол,
 * поднятый этой одной меткой, придавит ВСЕ следующие метки вверх до «сейчас»:
 * правка 2020 года приехала бы с меткой 2026, и подлинно более новая местная
 * правка соседа проиграла бы ей слияние (было: без надгробия первым —
 * P0 2020-01-01 cnt0; с ним первым — всё 2026-09-02). Присутствия по
 * created_at ASC, потом надгробия (тоже по времени, обычно позже любого
 * created_at) — это и есть настоящий хронологический порядок правок, и HLC
 * растёт по нему монотонно САМ, без скачков.
 *
 * Порядку ПРИМЕНЕНИЯ на приёме это не мешает: records.js разбирает put/del
 * по меткам и надгробиям независимо от порядка записей внутри порции — важен
 * только порядок ЧЕКАНКИ метки, а его задаёт именно эта функция.
 *
 * @param {import('better-sqlite3').Database} db
 * @param {number} limit
 * @param {{tbl: string|null, at: string, id: number}} cursor
 *   null tbl — самое начало (ни одна фаза ещё не пройдена).
 * @returns {{heads: Array, cursor: object, done: boolean}}
 */
function seedPage(db, limit, cursor, started) {
  const out = [];
  let next = cursor;

  if (next.tbl !== TOMB_PHASE) {
    const rank = next.tbl ? TABLE_RANK[next.tbl] : -1;
    const rows = db.prepare(SEED_PRESENCE_SQL)
      .all({ at: next.at || '', rank, id: next.id || 0, limit, started });
    for (const r of rows) out.push({ tbl: r.tbl, uid: r.uid, seq: 0, at: r.at });
    // rows.length === limit — страница заполнена целиком присутствиями,
    // дальше ещё могут быть; курсор остаётся в этой фазе. rows.length < limit
    // (в том числе 0) — присутствия ТОЧНО исчерпаны, переходим к надгробиям
    // с нуля — в этой же странице, если бюджет остался.
    next = rows.length === limit
      ? { tbl: rows[rows.length - 1].tbl, at: rows[rows.length - 1].at, id: rows[rows.length - 1].id }
      : { tbl: TOMB_PHASE, at: '', id: 0 };
  }

  if (out.length < limit && next.tbl === TOMB_PHASE) {
    // seq, а не rowid (ревью Задачи 4, N1 — воспроизведено ревью): rowid без
    // AUTOINCREMENT переиспользуется, как только pruneJournal вычищает
    // sync_tombstones целиком, и курсор соседа, остановившийся посреди этой
    // фазы, молча пропускает новое надгробие с переиспользованным номером.
    const rows = db.prepare(`
      SELECT tbl, uid, at, seq FROM sync_tombstones
       WHERE seq > ? AND at <= ? ORDER BY seq LIMIT ?
    `).all(next.id, started, limit - out.length);
    for (const r of rows) out.push({ tbl: r.tbl, uid: r.uid, seq: 0, at: r.at });
    if (rows.length) next = { tbl: TOMB_PHASE, at: '', id: rows[rows.length - 1].seq };
  }

  // out.length < limit (а не «обе фазы явно исчерпаны») — значит, ровно
  // limit оставшихся строк стоят одним лишним пустым обходом, зато не нужно
  // отдельно спрашивать «а сколько ещё» до самой отдачи. Пейджинг это уже
  // терпит везде в этом файле (тот же приём у тёплого хвоста).
  return { heads: out, cursor: next, done: out.length < limit };
}

// Хвост, ПОДТВЕРЖДЁННЫЙ всеми соседями, больше не нужен: без чистки журнал растёт
// ~4.7 млн строк в год на клинике с 300 визитами в день, а сборка порции
// сканирует его целиком. Сосед, молчавший дольше STALE_DAYS, забывается: его
// строка удаляется, и по возвращении он получает холодный засев из таблиц —
// иначе он навсегда пропустил бы всё, что вычищено за время его отсутствия.
// Удалять строку ОБЯЗАТЕЛЬНО раньше, чем считать пол: если просто исключить
// такого соседа из MIN(sent_seq), его собственная sync_peers-строка
// (старый sent_seq) остаётся на месте — buildBatch увидит её, посчитает
// соседа тёплым и станет читать хвост журнала НИЖЕ уже вычищенного места:
// дыра, а не переотправка.
//
// ПОЛ — ПО ПОДТВЕРЖДЁННОМУ (Задача 7b), а не по выложенному: журнальная
// запись, о которой сосед не отчитался, обязана дожить до следующего блоба,
// иначе вся эта задача бессмысленна. Цена названа вслух: сосед, который
// принимает, но не выкладывает свой блоб (а значит, и квитанции), держит
// журнал целиком, пока не станет STALE_DAYS-молчуном и не будет забыт. Это
// та же граница, что и раньше, — просто теперь она сторожит и чистку.
const STALE_DAYS = 30;
// Надгробия переживают забытого соседа ДОЛЬШЕ, чем сама запись о нём (ревью
// Задачи 4, C2): удалить строку из sync_tombstones раньше, чем сосед успеет
// вернуться и забрать холодный засев, значит воскресить у него ту строку,
// которую мы у себя давно удалили — обычный put её просто не защитит, надеть
// защиту нечем. 2×STALE_DAYS — не строгая гарантия (сосед может молчать и
// дольше), а осознанно ОГРАНИЧЕННЫЙ риск: молчание дольше 60 дней — это уже
// не «не успели снять надгробие», а «филиал закрылся или сменил адрес», и
// хранить надгробия для него вечно дороже, чем один раз пересверить руками.
const TOMBSTONE_DAYS = STALE_DAYS * 2;
export function pruneJournal(db, { now = () => new Date() } = {}) {
  const cutoff = new Date(now().getTime() - STALE_DAYS * 86400000).toISOString();
  // ЗАБЫВАЕМ ПО КВИТАНЦИИ, А НЕ ПО ВЫГРУЗКЕ (Задача 7b). last_ok — «когда МЫ
  // последний раз удачно выложились», и у соседа, выключенного навсегда, он
  // всё равно свежий: блоб-то на сервер лёг. Пока горизонты были одним числом,
  // это ничего не значило; теперь такой сосед держал бы и свой недосеянный
  // засев, и чистку нашего журнала ВЕЧНО. last_ack ставится при заведении
  // строки (фора новому соседу) и обновляется каждой пришедшей квитанцией.
  db.prepare('DELETE FROM sync_peers WHERE COALESCE(last_ack, last_ok) IS NULL OR COALESCE(last_ack, last_ok) < ?')
    .run(cutoff);

  const tombCutoff = new Date(now().getTime() - TOMBSTONE_DAYS * 86400000).toISOString();
  db.prepare('DELETE FROM sync_tombstones WHERE at < ?').run(tombCutoff);

  // I6 (ревью Задачи 4): соседей вообще не осталось — не было ни одного, или
  // все только что забыты выше. MIN(sent_seq) ниже был бы NULL, чистка
  // молчала бы НАВСЕГДА, и журнал рос бы у клиники без единого настроенного
  // соседа так же, как рос бы вовсе без этой функции.
  const remaining = db.prepare('SELECT COUNT(*) AS n FROM sync_peers').get().n;
  if (remaining === 0) return db.prepare('DELETE FROM sync_journal').run().changes;

  const floor = db.prepare('SELECT MIN(sent_seq) AS m FROM sync_peers WHERE last_ok IS NOT NULL AND last_ok >= ?').get(cutoff);
  if (!floor || floor.m == null || floor.m <= 0) return 0;
  return db.prepare('DELETE FROM sync_journal WHERE seq <= ?').run(floor.m).changes;
}

const CLOCK_KEY = 'sync_clock';

// control_state(key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at TEXT) —
// та же таблица и та же форма upsert, что в rpc/branch-sync.js (getState /
// putState); здесь не переиспользованы напрямую, потому что там не
// экспортированы, а тянуть ради двух строк весь модуль RPC — лишняя связь.

/** Последнее состояние часов из control_state. Строки → числа (TEXT-колонка). */
export function readClock(db) {
  const row = db.prepare('SELECT value FROM control_state WHERE key = ?').get(CLOCK_KEY);
  if (!row) return null;
  let v;
  try {
    v = JSON.parse(row.value);
  } catch {
    // Испорченная запись не должна ронять сборку порции — часы просто
    // начнутся заново от текущего времени, как при самом первом запуске.
    return null;
  }
  if (!v || typeof v !== 'object' || Array.isArray(v)) return null;
  const ms = Number(v.ms);
  const cnt = Number(v.cnt);
  if (!Number.isFinite(ms) || !Number.isFinite(cnt)) return null;
  return { ms, cnt };
}

/** Записать часы — только если ушли вперёд. После удачной отправки и после каждого приёма. */
export function writeClock(db, clock) {
  if (!clock) return;
  const prev = readClock(db);
  // Часы не откатываются НАЗАД записью: порция, собранная позже за то же
  // время (тот же ms, меньший или равный cnt), не должна вернуть часы назад —
  // иначе следующая метка могла бы повториться или уйти ниже уже отправленной.
  if (prev && (prev.ms > clock.ms || (prev.ms === clock.ms && prev.cnt >= clock.cnt))) return;
  db.prepare(`
    INSERT INTO control_state (key, value, updated_at)
    VALUES (?, ?, strftime('%Y-%m-%dT%H:%M:%SZ','now'))
    ON CONFLICT(key) DO UPDATE SET value = excluded.value, updated_at = excluded.updated_at
  `).run(CLOCK_KEY, JSON.stringify({ ms: clock.ms, cnt: clock.cnt }));
}

/**
 * ДВА ГОРИЗОНТА, А НЕ ОДИН (Задача 7b — подтверждённая доставка).
 *
 * До неё была одна отметка `markSent`, и ставилась она по ответу релея 2xx.
 * Но 2xx означает «блоб лежит на сервере», а не «сосед его прочитал»: блоб
 * узла ОДИН и замещается следующей выгрузкой, поэтому сосед, выключенный на
 * ночь, пропускал ВСЁ, что выложили за эту ночь, — отправитель второй раз это
 * не слал. Теперь событий два, и у каждого своя колонка в sync_peers:
 *
 *   markPublished — выложено (2xx). Двигает pub_seq, курсор засева, часы и
 *                   last_ok. pub_seq снимает защиту местной правки
 *                   (records.js localUnshippedCols).
 *   markConfirmed — подтверждено соседом (квитанция в его блобе). Двигает
 *                   sent_seq, по которому собирается срез и чистится журнал.
 *
 * Развести их ОБЯЗАТЕЛЬНО. Пробная сборка Задачи 7 двигала по квитанции один
 * общий sent_seq — и на задержке подтверждения защита местной правки (она
 * читала тот же sent_seq) держала строки с обеих сторон: B не принимал адрес
 * от C, C не принимал телефон от B, ни один не подтверждал, сеть не сходилась
 * вовсе. Защиту снимает ВЫГРУЗКА (наше авторство уже видно соседу, а спор
 * разбирают поколоночные метки), журнал держит ПОДТВЕРЖДЕНИЕ.
 *
 * seed — ОБЯЗАТЕЛЕН, когда buildBatch вернул его (сосед холодный или
 * засевается): это курсор страницы, а не необязательная деталь. Без него
 * этот вызов упадёт явной ошибкой, а не молча закроет незавершённый засев —
 * закрыть его молча значило бы навсегда потерять всё, что страница ещё не
 * долистала (ревью Задачи 4, C1: «5000 из 18000 пациентов, ноль визитов»).
 * Для тёплого соседа seed — null, как и раньше.
 *
 * @param {object|null} seed batch.seed из buildBatch, без изменений
 */
export function markPublished(db, peer, upto, clock = null, seed = null, { now = () => new Date() } = {}) {
  // I5 (ревью Задачи 4): всё внутри одной транзакции — состояние соседа,
  // часы и чистка журнала обязаны либо примениться целиком, либо не
  // примениться вовсе. Половина (например, pub_seq сдвинут, а чистка
  // журнала прервалась) — это ровно тот же класс дыры, что и остальной файл
  // старается не допустить.
  const run = db.transaction(() => {
    const existing = db.prepare('SELECT seed_floor, last_ok FROM sync_peers WHERE node = ?').get(peer);
    const wasSeeding = existing == null || existing.last_ok == null || existing.seed_floor != null;
    if (wasSeeding && !seed) {
      throw new Error(
        `journal: markPublished(${JSON.stringify(peer)}) called without seed info while the peer is cold or mid-seed`
      );
    }

    if (seed) {
      // КУРСОР ЗАСЕВА ЗДЕСЬ НЕ ДВИГАЕТСЯ (Задача 7b). Выгрузка означает «блоб
      // лежит на сервере», а страницу засева, которую сосед не забрал до
      // следующей выгрузки, надо выложить СНОВА — иначе у клиники на 70 000
      // пациентов в засеве остаётся дыра ровно там, где её никто не заметит.
      // Запоминаем только КОНЕЦ выложенной страницы; курсор переедет туда,
      // когда придёт квитанция с её номером (markConfirmed).
      //
      // seed_floor держится до подтверждения ПОСЛЕДНЕЙ страницы: сбрось его
      // здесь по seed.done — и незабранная последняя страница потерялась бы,
      // а сосед стал бы «тёплым» с непривезённым хвостом таблиц.
      // ПУСТАЯ И ПОСЛЕДНЯЯ страница закрывает засев прямо здесь: подтверждать
      // в ней нечего, доставлять — тоже. Ждать квитанции за ноль строк значило
      // бы ждать её вечно (см. seed.rows в buildBatch).
      const finished = seed.done && !seed.rows;
      db.prepare(`
        INSERT INTO sync_peers (node, pub_seq, last_ok, last_ack, seed_floor, seed_started,
                                seed_next_tbl, seed_next_at, seed_next_id, seed_next_done)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
        ON CONFLICT(node) DO UPDATE SET
          pub_seq = MAX(pub_seq, excluded.pub_seq), last_ok = excluded.last_ok,
          seed_floor = excluded.seed_floor, seed_started = excluded.seed_started,
          seed_next_tbl = excluded.seed_next_tbl, seed_next_at = excluded.seed_next_at,
          seed_next_id = excluded.seed_next_id, seed_next_done = excluded.seed_next_done
      `).run(
        peer, seed.floor, now().toISOString(), now().toISOString(),
        finished ? null : seed.floor, finished ? null : seed.started,
        finished ? null : seed.tbl, finished ? null : seed.at,
        finished ? 0 : seed.id, finished ? 0 : (seed.done ? 1 : 0),
      );
    } else {
      db.prepare(`
        INSERT INTO sync_peers (node, pub_seq, last_ok, last_ack) VALUES (?, ?, ?, ?)
        ON CONFLICT(node) DO UPDATE SET pub_seq = MAX(pub_seq, excluded.pub_seq), last_ok = excluded.last_ok
      `).run(peer, upto, now().toISOString(), now().toISOString());
    }
    writeClock(db, clock);
    pruneJournal(db, { now });
  });
  run();
}

/**
 * Квитанция соседа: он ПРИМЕНИЛ наш срез до `upto`. Только теперь эти записи
 * можно перестать повторять и вычистить из журнала.
 *
 * ОГРАНИЧИВАЕТСЯ pub_seq, и это не формальность: квитанция приезжает СНАРУЖИ,
 * из чужого блоба. Сосед со сломанной сборкой (или тот, кто подделал блоб)
 * прислал бы число из будущего, sent_seq перепрыгнул бы через ещё не
 * отправленные записи, а pruneJournal их бы стёр — молча и навсегда. Больше
 * выложенного не подтверждают.
 *
 * Строки нет — подтверждать нечего: соседу, которому мы ни разу не
 * выгружались, нечего было и получать (строку заводит markPublished).
 *
 * @returns {number} новый sent_seq (0 — ничего не изменилось)
 */
export function markConfirmed(db, peer, upto, seedPage = 0, { now = () => new Date() } = {}) {
  const ack = Math.floor(Number(upto));
  const page = Math.floor(Number(seedPage));
  const gotUpto = Number.isFinite(ack) && ack > 0;
  const gotPage = Number.isFinite(page) && page > 0;
  if (!gotUpto && !gotPage) return 0;

  const run = db.transaction(() => {
    const row = db.prepare('SELECT * FROM sync_peers WHERE node = ?').get(peer);
    if (!row) return 0;

    let sent = row.sent_seq;
    if (gotUpto) sent = Math.max(row.sent_seq, Math.min(ack, row.pub_seq));

    // СТРАНИЦА ЗАСЕВА ПОДТВЕРЖДЕНА — и только ровно СЛЕДУЮЩАЯ за
    // подтверждённой. Номер из будущего (сломанная сборка соседа, подделанный
    // блоб) курсор не двигает: перепрыгнув страницу, мы оставили бы у соседа
    // дыру, которую уже нечем закрыть.
    const advance = gotPage && row.seed_floor != null && page === row.seed_page + 1;
    if (advance && row.seed_next_done) {
      // Последняя страница принята — засев закончен. Дальше сосед тёплый и
      // читает хвост журнала выше пола, как обычно.
      db.prepare(`UPDATE sync_peers SET seed_floor = NULL, seed_started = NULL, seed_tbl = NULL,
                         seed_at = NULL, seed_id = 0, seed_page = 0, seed_next_tbl = NULL,
                         seed_next_at = NULL, seed_next_id = 0, seed_next_done = 0 WHERE node = ?`).run(peer);
    } else if (advance) {
      db.prepare(`UPDATE sync_peers SET seed_tbl = ?, seed_at = ?, seed_id = ?, seed_page = ?,
                         seed_next_tbl = NULL, seed_next_at = NULL, seed_next_id = 0, seed_next_done = 0
                   WHERE node = ?`)
        .run(row.seed_next_tbl, row.seed_next_at, row.seed_next_id, page, peer);
    }

    // Квитанция пришла — сосед жив и разговаривает. По этой дате его и
    // забывают (pruneJournal), а не по last_ok: тот обновляется на каждой
    // НАШЕЙ выгрузке и у выключенного навсегда соседа всё равно свежий.
    db.prepare('UPDATE sync_peers SET sent_seq = ?, last_ack = ? WHERE node = ?')
      .run(sent, now().toISOString(), peer);

    // Горизонт подтверждённого — это и есть пол чистки: сдвинулся он —
    // появилось, что чистить. В markPublished чистка теперь почти всегда
    // холостая, а настоящая работа у неё здесь.
    if (sent !== row.sent_seq) pruneJournal(db, { now });
    return sent;
  });
  return run();
}


/**
 * Оба события разом. Осталось РАДИ ВЫЗЫВАЮЩИХ, которым релей не нужен:
 * прямой обмен (Маршрут А) отвечает 2xx только после того, как сосед порцию
 * ПРИМЕНИЛ, — там «выложено» и «подтверждено» действительно одно событие.
 * Путь через релей обязан звать две функции раздельно: в нём между ними
 * проходит цикл, и вся Задача 7b именно про этот промежуток.
 */
export function markSent(db, peer, upto, clock = null, seed = null, opts = {}) {
  markPublished(db, peer, upto, clock, seed, opts);
  markConfirmed(db, peer, seed ? seed.floor : upto, seed ? seed.page : 0, opts);
}
