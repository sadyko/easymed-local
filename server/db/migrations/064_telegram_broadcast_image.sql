-- TELEGRAM_BROADCAST_IMG_V1 — картинка 16:9 к рассылке.
--
-- Хранится ПУТЁМ, а не байтами: файл лежит в data/storage/telegram-media/,
-- как и остальные загрузки клиники. Держать картинку в SQLite означало бы
-- раздувать файл базы ради данных, которые база не ищет и не соединяет.
--
-- NULL — рассылка без картинки, то есть ровно то поведение, что было до этой
-- миграции. Существующие строки поэтому менять не нужно.
ALTER TABLE telegram_broadcasts ADD COLUMN image_path TEXT;
