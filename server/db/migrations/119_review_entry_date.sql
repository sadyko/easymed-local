-- 119_review_entry_date.sql — DIARY_ENTRY_DATE_V1: ЗА КАКОЙ ДЕНЬ ЗАПИСЬ.
--
-- Владелец (2026-09-10) о дневнике наблюдения: «is one document which can be
-- added every day with same fields, and the date selected and saved».
--
-- Дневник пишут КАЖДЫЙ ДЕНЬ, и написан он сплошь и рядом утром следующего:
-- обход был в пятницу вечером, запись легла в субботу. Время публикации
-- отвечает на вопрос «когда напечатали», а лечение читают по «за какое число»
-- — и по нему же считается, пропущен ли день.
--
-- ПОЧЕМУ ОТДЕЛЬНАЯ КОЛОНКА. published_at — это подпись под документом, время
-- юридическое: подменив его вчерашней датой, мы получили бы документ,
-- подписанный задним числом. День записи — сведение врачебное, и живёт оно
-- рядом, а не вместо.
--
-- Пусто — как было: днём записи считается день публикации. Старые дневники
-- ничем не отличаются от новых.
--
-- Только ADD COLUMN — пересборок нет (урок 1.1.0).
ALTER TABLE admission_reviews ADD COLUMN entry_date TEXT;
