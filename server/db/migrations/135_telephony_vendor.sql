-- 135_telephony_vendor.sql — MOIZVONKI_V1: ВИД ПОДКЛЮЧЕНИЯ БЕЗ ПОТОЛКА.
--
-- Владелец (2026-09-17), про «Мои Звонки»: «do not hardcode the my calls. leave
-- only the fields for call and api (like in the pbx and binotel)». То есть
-- третья телефония настраивается теми же полями, что и onlinePBX, — а значит,
-- её строка должна ложиться в ту же таблицу подключений.
--
-- ПОЧЕМУ НОВАЯ КОЛОНКА, А НЕ ПРАВКА СТАРОЙ. В telephony_providers (миграция
-- 126) вид подключения ограничен намертво:
--
--     kind TEXT NOT NULL CHECK (kind IN ('onlinepbx'))
--
-- Расширить CHECK в SQLite нельзя: его меняют только пересборкой таблицы —
-- создать новую, перелить, СТАРУЮ УДАЛИТЬ, переименовать. Против этого здесь
-- два довода, и каждый достаточен сам по себе:
--   • правило миграций этого проекта — «только ADD COLUMN, CREATE TABLE,
--     INSERT»: настройки телефонии клиника вводит руками один раз, и удалять
--     таблицу с ними ради расширения списка нельзя;
--   • на эту таблицу смотрит внешний ключ calls.provider_id. Пересборка
--     оторвала бы уже собранные звонки от подключения, через которое они
--     приехали, — а это журнал за месяцы.
--
-- ПОЭТОМУ: настоящий вид живёт в новой колонке vendor, а kind остаётся
-- историей — у всех строк там 'onlinepbx', что бы ни было на самом деле.
-- Пустой vendor означает «onlinePBX»: старые строки не переписываются (UPDATE
-- в миграциях тоже не наш приём), их вид читается из kind как раньше.
--
-- КТО ЧИТАЕТ. Только providers.js, функцией providerKind(row) = vendor || kind.
-- Прямой запрос с фильтром по kind молча потеряет «Мои Звонки» — на это стоит
-- тест.
--
-- ТОЛЬКО ADD COLUMN.
ALTER TABLE telephony_providers ADD COLUMN vendor TEXT;
